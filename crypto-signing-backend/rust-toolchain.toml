# rustup pin for offline native regeneration. Channel is the exact
# toolchain recorded in README.md (verified 2026-08-23). Cross targets
# are added by the rebuild scripts (`rustup target add`), not here, so a
# `cargo metadata --locked` host check does not have to download them.
[toolchain]
channel = "1.97.0"
